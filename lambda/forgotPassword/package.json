{
  "name": "forgot-password",
  "version": "1.0.0",
  "description": "Lambda function to handle forgot password requests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0"
  }
}
